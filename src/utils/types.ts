export interface Post {
    id: string;
    company: string;
    place: string;
    status: string;
}

export interface Job {
    id: string;
    title: string;
    company: string;
    place: string;
}